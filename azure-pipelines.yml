trigger:
    - master

pool:
    vmImage: "windows-latest"

steps:
  - script: |
      npm run publish
    displayName: "Build and Release"
    env:
      GITHUB_TOKEN: $(GH_TOKEN)
      MARKETPLACE_TOKEN: $(PAT_TOKEN)
