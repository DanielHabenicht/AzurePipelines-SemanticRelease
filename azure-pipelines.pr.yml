trigger: none
pr:
  branches:
    include:
      - master

pool:
    vmImage: "ubuntu-latest"

steps:
- script: |
    npm install
  displayName: "Install"
- script: |
    npm run package
  displayName: "Build"
